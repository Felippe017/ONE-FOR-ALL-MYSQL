CREATE VIEW top_3_artistas AS SELECT a.nome_artista AS 'artista', COUNT(s.artista_id) AS 'seguidores' FROM SpotifyClone.artista AS a INNER JOIN SpotifyClone.seguidor AS s ON a.artista_id = s.artista_id GROUP BY s.artista_id ORDER BY COUNT(s.artista_id) DESC LIMIT 3;